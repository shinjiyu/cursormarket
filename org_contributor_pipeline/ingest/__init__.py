"""GitLab / git ingest for org_contributor_pipeline.

Use ``python -m org_contributor_pipeline.ingest.gitlab_commits`` (or console script
``org-contributor-gitlab-ingest``) to pull group commits into pipeline JSON.
"""
